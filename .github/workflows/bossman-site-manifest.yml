name: Bossman site manifest

on:
  pull_request:
  workflow_dispatch:
  push:

permissions:
  contents: read

jobs:
  manifest:
    if: github.event_name != 'push' || github.ref_name == github.event.repository.default_branch
    uses: iamjasonhill/bossman-site-contract/.github/workflows/site-manifest-contract.yml@2b495dbcd1f26698f24f1d07cb88bce09c9cd5e9
